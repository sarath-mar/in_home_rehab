{
  "name": "nuxt-app",
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "deploy-fb": " rd /s /q node_modules && npm i && cd .. && firebase deploy",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "devDependencies": {
    "@mdi/js": "^7.2.96",
    "@types/node": "^18",
    "firebase-admin": "^11.7.0",
    "firebase-functions": "^4.3.1",
    "firebase-functions-test": "^3.1.0",
    "nuxt": "^3.4.3",
    "nuxt-icon": "^0.4.0"
  },
  "dependencies": {
    "sass": "^1.62.1",
    "vuetify": "^3.1.11"
  }
}
